"""Grounded root cause hypothesis generation.

Nothing in this package may be called directly. Every path into the model runs
through :mod:`yieldloop.guardrails`, which is not bypassable by a caller flag.
"""
