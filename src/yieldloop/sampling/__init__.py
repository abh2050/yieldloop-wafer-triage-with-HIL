"""Active learning sample selection.

Pure functions only. Nothing in this package performs I/O, reads configuration,
or touches the database, so every selection rule is directly testable and the
label efficiency curve is reproducible from a seed.
"""
